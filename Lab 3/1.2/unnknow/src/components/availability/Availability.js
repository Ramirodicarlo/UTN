import React from "react";

const Availability = () => {
  return (
    <div>
      {beers.filter((available) => available === true);{

      })
      }
    </div>
  );
};

export default Availability;
